[project]
name = "openapi-transmog"
version = "0.2.0"
description = "Simple OpenAPI transmogrifier that produces Python code"
authors = [{ name = "Mia yun Ruse", email = "mia@yunru.se" }]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.9,<4"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
